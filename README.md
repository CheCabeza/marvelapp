# Marvel App

This is a practice code for fetching data from a API.

# Link

https://marvelapp-teal.vercel.app/

# To run the project (scripts)
After you clone the project run the command npm install.

The scripts you can run with npm (or yarn) are:

 - npm start (to deploy the app in localhost)
 - npm test (to see unit & component tests)

# Unit testing:
 - Jest 
 - React testing library

